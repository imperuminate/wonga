const LoginPage = require('../pageobjects/login.page')

describe('Go through the /data journey step', async () => {

    it('Login with existing credentials', async () => {

        await LoginPage.open()
        // await LoginPage.login('tbrvtbrv+75@gmail.com', 'Passw0rd')

        // browser.debug()
        
    });
});